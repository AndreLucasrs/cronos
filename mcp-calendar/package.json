{
  "name": "cronos-mcp-calendar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP companion server — generates real .ics reminder files from Cronos tasks. No external credentials.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.9.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.2.2"
  }
}
